apiVersion: openstack.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: openstack
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: openstack-credentials
      key: clouds.yaml
